extern print_i64(n: i64);

struct Foo {
    x: i64,
    y: i64,
}

struct Bar {
    #transparent f: Foo,
}

fn main() i64 {
    let #transparent b = Bar { Foo { 1, 2 } };
    (f.x + f.y) |> print_i64;
    return 0;
}