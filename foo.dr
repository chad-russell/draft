extern print_i64(n: i64);
extern print_f64(n: f64);

struct Foo {
    x: !T,
}

fn main() i64 {
    let f = Foo { x: 42 };
    print_i64(f.x);

    let g = Foo { x: 4.2 };
    print_f64(g.x);

    // f = g;
    
    return 0;
}