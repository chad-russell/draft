// extern print_i64(n: i64);
// extern print_str(ch: *u8, len: i64);
// extern putchar(ch: u8);

// fn print_string(s: string) {
    // let i = 0;
    // while i < s.len {
        // putchar(s[i]);
        // i = i + 1;
    // }
// }

fn main() {
}
