extern print_i64(n: i64);
// extern print_f64(n: f64);

// enum Option {
    // Some: _T!,
    // None,
// }

struct Foo { x: i64 }

fn foo(f: Foo) _ {
    return f.x;
}

fn pfoo(f: *Foo) _ {
    return f.x;
}

fn main() i64 {
    // let a = Option::Some(3i64);

    let f = Foo { 3 };
    foo(f);
    pfoo(&f);

    return 0;
}