extern print_i64(n: i64);

struct Foo {
    x: i64,
    y: i64,
}

struct Bar {
    f: Foo,
}

fn main() i64 {
    let foo = Foo { x: 1, y: 2 };
    let bar = Bar { foo };

    print_i64(bar.f.x + foo.y);
    
    return 0;
}