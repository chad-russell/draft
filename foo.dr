extern print_i64(n: i64);
extern print_f64(n: f64);

struct Foo {
    x: !T,
}

struct Bar {
    f: Foo!,
}

fn main() i64 {
    let b: Bar! = _{ _{ 123 } };
    print_i64(b.f.x);
    
    // let bb: Bar! = _{ _{ 1.23 } };
    // print_f64(b.f.x);
    
    return 0;
}