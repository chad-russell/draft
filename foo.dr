extern print_i64(n: i64);
extern print_f64(n: f64);

struct Foo {
    x: i64,
}

fn foo(f: *Foo) _ {
    return f.x;
}

fn main() i64 {
    let f = Foo { x: 444 };
    print_i64(foo(&f));
    return 0;
}