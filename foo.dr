extern print_i64(n: i64);

struct Foo { x: i64, y: i64 }

fn main() i64 {
    let f = Foo { 3, 8 };
    let g = f;
    let pb = &f;

    print_i64(f.x);
    print_i64(g.x);
    print_i64(pb.x);

    return 0;
}