extern print_i64(n: i64);

struct Foo {
    x: i64,
    y: i64,
}

struct Bar {
    f: Foo,
}

fn sum_foo(f: *Foo) i64 {
    return f.x + f.y;
}

fn main() i64 {
    let f = Foo { 3, 4 };
    sum_foo(&f);
    
    return 0;
}