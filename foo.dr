extern print_i64(n: i64);
extern print_str(ch: *u8, len: i64);

fn print_string(s: string) {
    print_str(s.data, s.len);
    return;
}

struct Foo {
    m: i64,
    n: i64,
}

fn main() i64 {
    let f = Foo { 3, 4 };

    let t = #type_info(Foo);

    return 0;
}
