extern print_i64(n: i64);

struct Foo {
    x: i64,
    y: i64,
}

struct MyThing {
    #transparent get_foo: fn() Foo,
}

fn gf() _ {
    return Foo { 3, 4 };
}

fn main() i64 {
    let mt = MyThing {
        get_foo: gf,
    };

    print_i64(mt.x);

    return 0;
}
