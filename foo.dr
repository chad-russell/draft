extern print_i64(n: i64);
extern print_f64(n: f64);

struct Foo {
    x: !T,
}

struct Bar {
    f: Foo,
}

fn main() i64 {
    let b: Bar!;
    b.f = _{ 314 };
    print_i64(bbf.x);
    
    return 0;
}